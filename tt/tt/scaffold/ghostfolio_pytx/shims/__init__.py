"""Shim modules copied into app/implementation/_support/ by the runner.

These are pure-Python, stdlib-only stand-ins for the JS libraries the
TypeScript sources import (big.js, date-fns, lodash, @nestjs/common, and
a handful of Ghostfolio helpers). They live in the project-specific
scaffold, not in the generic tt/ core.
"""
